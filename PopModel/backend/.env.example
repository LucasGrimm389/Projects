# Backend environment variables for pop.ai (PopModel)

# Anthropic API key (required)
# Obtain from https://console.anthropic.com
POPMODEL_API_KEY=

# Google OAuth client ID (recommended)
# Create a Web application credential in Google Cloud Console
# Used to verify Google ID tokens on the backend
GOOGLE_CLIENT_ID=

# Admin backdoor code (optional)
# Default: Pop91525
ADMIN_CODE=Pop91525

# Local development bypass for auth (optional)
# When set to true, /api/message and history endpoints allow requests without Google ID tokens
# DO NOT use in production
ALLOW_INSECURE_NOAUTH=true

# Port (optional)
PORT=5001
# Copy this file to .env and set your key
# Never commit real secrets. .env is gitignored by default.

POPMODEL_API_KEY=
# or CLAUDE_API_KEY=

# Optional server port
# PORT=5001

# Optional model override (default: claude-3-5-sonnet-20240620)
# POPMODEL_MODEL=claude-3-5-sonnet-latest
